import { DirectiveSortDirective } from './directive-sort.directive';

describe('DirectiveSortDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveSortDirective();
    expect(directive).toBeTruthy();
  });
});
